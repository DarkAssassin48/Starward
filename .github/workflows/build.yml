name: Build

on:
  pull_request:
    branches: [ "main", "dev/*" ]

permissions:
  contents: write

jobs:
  apply_buff_ui:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout branch
      uses: actions/checkout@v5
      with:
        ref: fix/localized-time-units-0.18.0
        fetch-depth: 2

    - name: Apply buff UI
      run: python .github/scripts/apply_star_rail_buff_ui.py

    - name: Commit page changes
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git add src/Starward/Features/GameRecord/StarRail/PureFictionPage.xaml
        git add src/Starward/Features/GameRecord/StarRail/ApocalypticShadowPage.xaml
        git commit -m "Show selected buffs for Pure Fiction and Apocalyptic Shadow"
        git push
